const assert = require('assert');
const request = require('supertest');
const index = require("../app/src/index");

describe('router exists', () => {
    it('should have home (/) page', () => {

    });
});